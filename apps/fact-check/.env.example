# Copy to .env.local (gitignored). Vite inlines these into the bundle at build time.
# This is the glasses' own read-only credential — NOT the OpenAI or Slack token.
# It is still a secret in a web page: never pack it, never submit this app to Even Hub.
VITE_HUD_BASE=https://ais-mac-mini.tail7ed4e6.ts.net/hud
VITE_HUD_TOKEN=
